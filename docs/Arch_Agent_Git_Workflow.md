# Agent Git 工作流架构

> 版本：v1.1
> 更新日期：2026-01-20
> 状态：已定稿
>
> **设计目标**：Agent 只需最少 MCP 工具，复杂逻辑由 Server 处理
> **核心原则**：用户保持控制权，决定是否接受 AI 的修改

---

## 一、概述

### 1.1 设计目标

本架构定义 Agent 执行 Git 操作所需的工具链，遵循以下原则：

| 原则 | 说明 |
|------|------|
| **极简工具** | Agent 只需 2 个 MCP 工具 |
| **Server 内置** | 复杂逻辑（自动存档、获取当前分支）由 Server API 内部处理 |
| **用户控制** | 合并/清理由用户通过 Web UI 决定，Agent 不直接调用 |

### 1.2 Agent 工具清单

| MCP 工具 | 对应 Server API | 职责 |
|----------|----------------|------|
| `ai_job_create` | `POST /api/git/ai-job` | 为 SubAgent 创建隔离工作环境 |
| `ai_job_complete` | `POST /api/git/ai-job/{name}/complete` | 标记完成，通知 Web 端供用户审查 |

### 1.3 Agent 不负责的操作

| 操作 | 处理方式 | 说明 |
|------|----------|------|
| 获取当前分支 | Server API 内部 | `ai_job_create` 不传 baseBranch 时自动获取 |
| 自动存档 | Server API 内部 | 创建 Worktree 前自动执行 |
| git commit | 合并 API 内部 | 用户点击合并时自动执行 |
| 合并 (`merge`) | 用户通过 Web UI | Agent 生成总结后，用户决定是否合并 |
| 清理 (`worktrees DELETE`) | 用户通过 Web UI | 合并后或丢弃时清理 |

---

## 二、MCP 工具定义

### 2.1 ai_job_create - 创建 AI Job

为 SubAgent 创建隔离工作环境。

**Server API**：`POST /api/git/ai-job`

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `name` | string | ✅ | AI Job 名称，如 "layout-job-1" |
| `baseBranch` | string | ❌ | 基准分支，不传则 Server 自动使用当前分支 |

**返回值**：

```json
{
    "worktreePath": "E:/project/.worktrees/layout-job-1",
    "branchName": "feat/layout-job-1-20260120-143052"
}
```

**Server 内部逻辑**：

1. `baseBranch` 为空 → 调用 `GET /api/git/current` 获取当前分支
2. 检测未提交更改 → 自动存档（`git add . && git commit`）
3. 创建临时分支 + Worktree

**MCP 工具定义（Python）**：

```python
@mcp_tool()
async def ai_job_create(name: str, base_branch: str = None) -> dict:
    """
    为 SubAgent 创建隔离工作环境

    Args:
        name: AI Job 名称，如 "layout-job-1"
        base_branch: 基准分支（可选，不传则 Server 自动使用当前分支）

    Returns:
        {
            "worktreePath": "E:/project/.worktrees/layout-job-1",
            "branchName": "feat/layout-job-1-20260120-143052"
        }
    """
    # POST /api/git/ai-job { name, baseBranch? }
```

### 2.2 ai_job_complete - 标记 AI Job 完成

标记 AI Job 完成，通知 Web 端供用户审查。

**Server API**：`POST /api/git/ai-job/{name}/complete`

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `summary` | string | ✅ | 修改总结（展示给用户） |

**返回值**：

```json
{
    "success": true,
    "message": "AI Job 已完成，等待用户审查"
}
```

**Server 内部逻辑**：

1. 在 Worktree 中执行 `git add . && git commit`（提交未暂存的修改）
2. 通知 Web 端该 AI Job 已完成
3. Web 端展示 diff/合并按钮
4. 用户点击合并 → 调用合并 API

**MCP 工具定义（Python）**：

```python
@mcp_tool()
async def ai_job_complete(name: str, summary: str) -> dict:
    """
    标记 AI Job 完成，通知 Web 端供用户审查

    Args:
        name: AI Job 名称（同 ai_job_create 返回的 name）
        summary: 修改总结（展示给用户）

    Returns:
        {
            "success": true,
            "message": "AI Job 已完成，等待用户审查"
        }
    """
    # POST /api/git/ai-job/{name}/complete { summary }
```

---

## 三、业务场景工作流

> 本节定义 7 个完整业务场景，覆盖 Query/Execute 任务、多方案生成、窗口管理等全部情况。
> 每个场景提供真窗口（R）和虚拟窗口（V）两种流程图。

### 3.0 场景总览

| 场景 | 触发条件 | 说明 |
|------|----------|------|
| 场景1 | Query任务（只读） | 用户问"有多少家具"等只读问题 |
| 场景2 | 简单编辑 | 用户说"把床向右移动50cm"等单一修改 |
| 场景3 | 策略分叉 | 用户说"给我客厅出三个方案" |
| 场景4 | 布局求解器 | 用户说"能不能塞进浴缸和淋浴房" |
| 场景5 | 主编式合并 | 用户在三联屏中勾选不同方案的不同区域 |
| 场景6 | 多窗口独立任务 | 用户在不同窗口同时操作不同策略 |
| 场景7 | 虚拟窗口关闭 | 用户关闭虚拟窗口时的处理流程 |

### 3.1 场景1：Query 任务（只读）

**触发条件**：用户问"有多少家具"、"床在哪里"等只读问题

**特点**：无需 Git 操作，Agent 直接读取当前工作目录的文件。

#### 真窗口流程（R）

```
┌─────────────────────────────────────────────────────────────────┐
│  用户："主卧有多少家具？"                                        │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ MainAgent 判断：Query 任务，无需创建 AI Job                │  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ MCP: canvas_read_project(projectPath)                     │  │
│  │ → 读取 .bcp 项目文件中的家具数据                          │  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ Agent 回复："主卧有5件家具：床、床头柜x2、衣柜、梳妆台"   │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ★ 无 Git 操作，无分支切换，直接读取当前工作目录                │
└─────────────────────────────────────────────────────────────────┘
```

#### 虚拟窗口流程（V）

```
┌─────────────────────────────────────────────────────────────────┐
│  【与真窗口相同】                                                │
│                                                                 │
│  区别：读取的是虚拟窗口 Worktree 目录下的文件                    │
│  Agent 上下文已由 Server 设置好工作目录                          │
└─────────────────────────────────────────────────────────────────┘
```

---

### 3.2 场景2：简单编辑（单一修改）

**触发条件**：用户说"把床向右移动50cm"、"删掉这个椅子"等单一修改

#### 真窗口流程（R）

```
┌─────────────────────────────────────────────────────────────────┐
│  用户："把主卧的床向右移动50cm"                                  │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【R1】MainAgent 判断：Execute 任务，需要创建 AI Job        │  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【R2】MCP: ai_job_create(name="move-bed-1")               │  │
│  │ → Server 内部：                                            │  │
│  │     1. 获取当前分支（如 master）                           │  │
│  │     2. 检测未提交更改 → 自动存档                           │  │
│  │     3. 创建临时分支 feat/move-bed-1-20260120-xxx          │  │
│  │     4. 创建 Worktree                                       │  │
│  │ → 返回: { worktreePath, branchName }                       │  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【R3】SubAgent 在 worktreePath 中执行修改                  │  │
│  │ MCP: canvas_move_furniture(id, dx=500, dy=0)              │  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【R4】MCP: ai_job_complete(                               │  │
│  │   name="move-bed-1",                                       │  │
│  │   summary="已将主卧床向右移动50cm"                         │  │
│  │ )                                                          │  │
│  │ → Server 内部：git commit + 通知 Web                       │  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【R5】Web 端展示 diff 预览                                 │  │
│  │ 用户点击【合并】→ POST /api/git/merge                     │  │
│  │ 用户点击【丢弃】→ DELETE /api/git/worktrees/{name}        │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ★ Agent 对话结束后，不调用 merge/cleanup                       │
│  ★ 合并/丢弃决策权完全交给用户                                  │
└─────────────────────────────────────────────────────────────────┘
```

#### 虚拟窗口流程（V）

```
┌─────────────────────────────────────────────────────────────────┐
│  用户在虚拟窗口中："把床向右移动50cm"                            │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【V1】前置条件：虚拟窗口已存在                              │  │
│  │ 虚拟窗口 = Worktree + 绑定分支（如 test/virtual-window）   │  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【V2】MCP: ai_job_create(                                 │  │
│  │   name="move-bed-v",                                       │  │
│  │   base_branch="test/virtual-window"  ← 基于虚拟窗口分支    │  │
│  │ )                                                          │  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【V3】SubAgent 在 AI Job 的 worktreePath 中执行修改        │  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【V4】MCP: ai_job_complete(name="move-bed-v", summary=".")│  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【V5】用户审查 ★ 关键区别 ★                               │  │
│  │ POST /api/git/merge {                                      │  │
│  │   sourceBranch: "{branchName}",                            │  │
│  │   worktreeName: "virtual-window"  ← 合并到虚拟窗口！       │  │
│  │ }                                                          │  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【V6】清理 AI Job Worktree                                 │  │
│  │ DELETE /api/git/worktrees/move-bed-v?deleteBranch=true    │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

### 3.3 场景3：策略分叉（多方案生成）

**触发条件**：用户说"给我客厅出三个方案"、"尝试不同的布局风格"

#### 真窗口流程（R）

```
┌─────────────────────────────────────────────────────────────────┐
│  用户："给我出3个方案：极致收纳、动线优先、极简留白"              │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【R1】MainAgent 规划：需要 3 个并行 AI Job                 │  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                       │
│          ┌──────────────┼──────────────┐                        │
│          ▼              ▼              ▼                        │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐               │
│  │ 【R2a】     │ │ 【R2b】     │ │ 【R2c】     │               │
│  │ ai_job_    │ │ ai_job_    │ │ ai_job_    │               │
│  │ create(    │ │ create(    │ │ create(    │               │
│  │  "storage")│ │  "flow")   │ │  "minimal")│               │
│  └─────────────┘ └─────────────┘ └─────────────┘               │
│          │              │              │                        │
│          ▼              ▼              ▼                        │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐               │
│  │ 【R3a】     │ │ 【R3b】     │ │ 【R3c】     │               │
│  │ SubAgent   │ │ SubAgent   │ │ SubAgent   │               │
│  │ 极致收纳   │ │ 动线优先   │ │ 极简留白   │               │
│  └─────────────┘ └─────────────┘ └─────────────┘               │
│          │              │              │                        │
│          ▼              ▼              ▼                        │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐               │
│  │ 【R4a】     │ │ 【R4b】     │ │ 【R4c】     │               │
│  │ ai_job_    │ │ ai_job_    │ │ ai_job_    │               │
│  │ complete   │ │ complete   │ │ complete   │               │
│  └─────────────┘ └─────────────┘ └─────────────┘               │
│          │              │              │                        │
│          └──────────────┼──────────────┘                        │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【R5】Web 端三联屏展示                                     │  │
│  │ ┌─────────┐ ┌─────────┐ ┌─────────┐                       │  │
│  │ │ 方案A   │ │ 方案B   │ │ 方案C   │                       │  │
│  │ │ storage │ │ flow    │ │ minimal │                       │  │
│  │ │ [合并]  │ │ [合并]  │ │ [合并]  │                       │  │
│  │ │ [丢弃]  │ │ [丢弃]  │ │ [丢弃]  │                       │  │
│  │ └─────────┘ └─────────┘ └─────────┘                       │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ★ 用户可选择其中一个方案合并，或全部丢弃重新生成                │
│  ★ 也可进入"场景5：主编式合并"组合多方案                        │
└─────────────────────────────────────────────────────────────────┘
```

#### 虚拟窗口流程（V）

```
┌─────────────────────────────────────────────────────────────────┐
│  【与真窗口流程相同】                                            │
│                                                                 │
│  区别：                                                         │
│  - base_branch 使用虚拟窗口绑定的分支                            │
│  - 合并时使用 worktreeName 参数指向虚拟窗口                      │
│  - 三个方案的合并目标都是虚拟窗口，而非主仓库                    │
└─────────────────────────────────────────────────────────────────┘
```

---

### 3.4 场景4：布局求解器

**触发条件**：用户说"能不能塞进浴缸和淋浴房"、"这个空间放得下双人床吗"

#### 真窗口流程（R）

```
┌─────────────────────────────────────────────────────────────────┐
│  用户："卫生间能不能同时放浴缸和淋浴房？"                        │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【R1】MainAgent 判断：布局可行性分析任务                   │  │
│  │ 需要尝试多种布局方案                                       │  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【R2】MCP: ai_job_create(name="layout-solver-1")          │  │
│  │ → 创建隔离环境进行布局尝试                                 │  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【R3】SubAgent 执行布局求解                                │  │
│  │                                                            │  │
│  │ Step 1: 读取卫生间边界和禁区                               │  │
│  │ MCP: canvas_read_room(roomId="bathroom-1")                │  │
│  │                                                            │  │
│  │ Step 2: 获取浴缸和淋浴房的尺寸要求                         │  │
│  │ 浴缸: 1700x800mm，淋浴房: 900x900mm                        │  │
│  │                                                            │  │
│  │ Step 3: 尝试布局方案                                       │  │
│  │ ┌───────────────────────────────────────────┐              │  │
│  │ │ 尝试1: 浴缸靠北墙，淋浴房靠西南角         │              │  │
│  │ │ → 检测碰撞 → 通过 ✓                        │              │  │
│  │ └───────────────────────────────────────────┘              │  │
│  │ ┌───────────────────────────────────────────┐              │  │
│  │ │ 尝试2: 浴缸靠东墙，淋浴房靠西墙           │              │  │
│  │ │ → 检测碰撞 → 与门禁区冲突 ✗               │              │  │
│  │ └───────────────────────────────────────────┘              │  │
│  │                                                            │  │
│  │ Step 4: 选择最优方案并放置                                 │  │
│  │ MCP: canvas_place_module(...)                             │  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【R4】MCP: ai_job_complete(                               │  │
│  │   name="layout-solver-1",                                  │  │
│  │   summary="可以放置。浴缸靠北墙(1700x800)，               │  │
│  │            淋浴房靠西南角(900x900)，保留80cm通道。"        │  │
│  │ )                                                          │  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【R5】Web 端展示布局预览                                   │  │
│  │ ┌─────────────────────────────────────────┐                │  │
│  │ │ 布局方案预览（SVG 渲染）                 │                │  │
│  │ │  ┌────────────┐                         │                │  │
│  │ │  │   浴缸     │ ← 北墙                  │                │  │
│  │ │  └────────────┘                         │                │  │
│  │ │  ┌────┐                                 │                │  │
│  │ │  │淋浴│ ← 西南角                        │                │  │
│  │ │  │房  │                                 │                │  │
│  │ │  └────┘                                 │                │  │
│  │ └─────────────────────────────────────────┘                │  │
│  │ [应用此方案]  [调整方案]  [放弃]                           │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

#### 虚拟窗口流程（V）

```
┌─────────────────────────────────────────────────────────────────┐
│  【与真窗口流程相同】                                            │
│                                                                 │
│  区别：                                                         │
│  - ai_job_create 的 base_branch 使用虚拟窗口分支                 │
│  - 布局结果合并到虚拟窗口而非主仓库                              │
└─────────────────────────────────────────────────────────────────┘
```

---

### 3.5 场景5：主编式合并

**触发条件**：用户在三联屏中勾选不同方案的不同区域，希望组合多个方案的优点

#### 真窗口流程（R）

```
┌─────────────────────────────────────────────────────────────────┐
│  前置条件：场景3 已生成 3 个方案（storage、flow、minimal）       │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【R1】Web 端三联屏展示，用户进行区域选择                   │  │
│  │                                                            │  │
│  │ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐           │  │
│  │ │ 方案A       │ │ 方案B       │ │ 方案C       │           │  │
│  │ │ storage     │ │ flow        │ │ minimal     │           │  │
│  │ │             │ │             │ │             │           │  │
│  │ │ [✓] 客厅   │ │ [ ] 客厅   │ │ [ ] 客厅   │           │  │
│  │ │ [ ] 主卧   │ │ [✓] 主卧   │ │ [ ] 主卧   │           │  │
│  │ │ [ ] 次卧   │ │ [ ] 次卧   │ │ [✓] 次卧   │           │  │
│  │ └─────────────┘ └─────────────┘ └─────────────┘           │  │
│  │                                                            │  │
│  │ 用户选择：客厅用A、主卧用B、次卧用C                        │  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【R2】Web 调用合并接口                                     │  │
│  │ POST /api/git/cherry-pick-regions                         │  │
│  │ {                                                          │  │
│  │   "selections": [                                          │  │
│  │     { "region": "客厅", "sourceBranch": "feat/storage-*" },│  │
│  │     { "region": "主卧", "sourceBranch": "feat/flow-*" },   │  │
│  │     { "region": "次卧", "sourceBranch": "feat/minimal-*" } │  │
│  │   ],                                                       │  │
│  │   "targetBranch": "master"                                 │  │
│  │ }                                                          │  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【R3】Server 执行区域级合并                                │  │
│  │                                                            │  │
│  │ 内部逻辑：                                                 │  │
│  │ 1. 从各方案分支提取对应区域的 JSON 数据                    │  │
│  │ 2. 组合成新的完整布局 JSON                                 │  │
│  │ 3. 写入目标分支                                            │  │
│  │ 4. 创建合并 commit                                         │  │
│  │                                                            │  │
│  │ ★ 这是 JSON 级别的合并，不是 Git 文件级别的合并            │  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【R4】清理所有临时 AI Job Worktree                         │  │
│  │ DELETE /api/git/worktrees/storage?deleteBranch=true       │  │
│  │ DELETE /api/git/worktrees/flow?deleteBranch=true          │  │
│  │ DELETE /api/git/worktrees/minimal?deleteBranch=true       │  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【R5】Canvas 刷新，显示组合后的最终布局                    │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ★ Agent 不参与此流程，完全由 Web 端和 Server 协作完成          │
│  ★ 这是"用户保持控制权"原则的典型体现                          │
└─────────────────────────────────────────────────────────────────┘
```

#### 虚拟窗口流程（V）

```
┌─────────────────────────────────────────────────────────────────┐
│  【与真窗口流程相同】                                            │
│                                                                 │
│  区别：                                                         │
│  - cherry-pick-regions 的 targetBranch 改为 worktreeName        │
│  - 组合结果写入虚拟窗口的分支                                    │
│                                                                 │
│  POST /api/git/cherry-pick-regions                              │
│  {                                                              │
│    "selections": [...],                                         │
│    "worktreeName": "virtual-window"  ← 组合到虚拟窗口           │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
```

---

### 3.6 场景6：多窗口独立任务

**触发条件**：用户在不同窗口同时操作不同策略

#### 流程图

```
┌─────────────────────────────────────────────────────────────────┐
│  用户同时打开 3 个窗口，操作不同策略                             │
│                                                                 │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 【真窗口】主仓库视图                                       │ │
│  │ 分支: master                                               │ │
│  │ 工作目录: E:/project                                       │ │
│  │                                                            │ │
│  │ 用户："主卧加一个床头柜"                                   │ │
│  │ → ai_job_create("nightstand-1", baseBranch=null)          │ │
│  │ → Server 自动获取 master 作为基准                          │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 【虚拟窗口A】策略A - 极简风格                              │ │
│  │ 分支: strategy/minimal-20260120                            │ │
│  │ 工作目录: E:/project/.worktrees/window-a                   │ │
│  │                                                            │ │
│  │ 用户："把客厅的沙发换成单人椅"                             │ │
│  │ → ai_job_create("chair-swap", baseBranch="strategy/min.") │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 【虚拟窗口B】策略B - 豪华风格                              │ │
│  │ 分支: strategy/luxury-20260120                             │ │
│  │ 工作目录: E:/project/.worktrees/window-b                   │ │
│  │                                                            │ │
│  │ 用户："加一个水晶吊灯"                                     │ │
│  │ → ai_job_create("chandelier", baseBranch="strategy/lux.") │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ★ 三个任务完全独立，互不影响                                   │
│  ★ 每个窗口的 Agent 上下文由 Server 隔离                        │
│  ★ 合并时各自合并到对应窗口的分支                               │
└─────────────────────────────────────────────────────────────────┘
```

#### 关键设计

| 窗口类型 | 分支来源 | 合并目标 | Agent 上下文 |
|----------|----------|----------|--------------|
| 真窗口 | master（或当前活动分支） | master | 主仓库目录 |
| 虚拟窗口A | strategy/minimal-xxx | strategy/minimal-xxx | Worktree A |
| 虚拟窗口B | strategy/luxury-xxx | strategy/luxury-xxx | Worktree B |

**Agent 如何区分窗口？**

Agent **不需要知道**自己在哪个窗口：
- Server 在启动 Agent 时已设置好工作目录上下文
- `ai_job_create` 不传 `baseBranch` 时，Server 自动获取当前上下文的分支
- 合并操作由用户通过 Web UI 触发，Web 知道窗口类型

---

### 3.7 场景7：虚拟窗口关闭

**触发条件**：用户关闭虚拟窗口

#### 流程图

```
┌─────────────────────────────────────────────────────────────────┐
│  用户点击虚拟窗口的【关闭】按钮                                  │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【1】Web 检测是否有未合并的更改                            │  │
│  │                                                            │  │
│  │ GET /api/git/worktrees/{name}/status                      │  │
│  │ → { hasUnmergedChanges: true/false }                       │  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                       │
│          ┌──────────────┴──────────────┐                        │
│          ▼                             ▼                        │
│  ┌─────────────────────┐      ┌─────────────────────┐          │
│  │ 无未合并更改        │      │ 有未合并更改        │          │
│  │                     │      │                     │          │
│  │ 直接进入【3】清理   │      │ 进入【2】确认对话框 │          │
│  └─────────────────────┘      └─────────────────────┘          │
│                                        │                        │
│                                        ▼                        │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【2】显示确认对话框                                        │  │
│  │                                                            │  │
│  │ ┌─────────────────────────────────────────────────────┐   │  │
│  │ │ 虚拟窗口 "window-a" 有未合并的更改                  │   │  │
│  │ │                                                     │   │  │
│  │ │ 包含以下 AI Job:                                    │   │  │
│  │ │ • chair-swap: "把客厅沙发换成单人椅"               │   │  │
│  │ │ • table-add: "添加茶几"                            │   │  │
│  │ │                                                     │   │  │
│  │ │ [合并到主分支后关闭]  [丢弃更改并关闭]  [取消]      │   │  │
│  │ └─────────────────────────────────────────────────────┘   │  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                       │
│          ┌──────────────┼──────────────┐                        │
│          ▼              ▼              ▼                        │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐               │
│  │ 合并后关闭 │ │ 丢弃并关闭 │ │ 取消       │               │
│  │             │ │             │ │             │               │
│  │ POST merge │ │ 直接清理   │ │ 返回窗口   │               │
│  │ 然后清理   │ │             │ │             │               │
│  └─────────────┘ └─────────────┘ └─────────────┘               │
│          │              │                                       │
│          └──────────────┘                                       │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【3】清理虚拟窗口                                          │  │
│  │                                                            │  │
│  │ Step 1: 清理关联的 AI Job Worktree（如有）                 │  │
│  │ DELETE /api/git/worktrees/chair-swap?deleteBranch=true    │  │
│  │ DELETE /api/git/worktrees/table-add?deleteBranch=true     │  │
│  │                                                            │  │
│  │ Step 2: 清理虚拟窗口 Worktree                              │  │
│  │ DELETE /api/git/worktrees/window-a?deleteBranch={选择}    │  │
│  │                                                            │  │
│  │ deleteBranch 选项：                                        │  │
│  │ • true  - 如果用户选择"丢弃"，删除分支                    │  │
│  │ • false - 如果用户选择"合并"，保留分支历史                │  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                       │
│                         ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 【4】更新 UI                                               │  │
│  │                                                            │  │
│  │ • 从窗口列表中移除该虚拟窗口                               │  │
│  │ • 如果合并了更改，刷新真窗口的 Canvas                      │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ★ Agent 不参与此流程                                           │
│  ★ 完全由 Web 端 + Server 协作完成                              │
│  ★ 用户始终拥有最终决策权                                       │
└─────────────────────────────────────────────────────────────────┘
```

---

### 3.8 真窗口 vs 虚拟窗口 关键区别

| 维度 | 真窗口 | 虚拟窗口 |
|------|--------|----------|
| 分支基准 | 主仓库当前分支（如 master） | 虚拟窗口绑定的分支 |
| 合并参数 | `targetBranch: "master"` | `worktreeName: "window-a"` |
| 合并位置 | 主仓库目录 | 虚拟窗口 Worktree 目录 |
| 工作目录 | `E:/project` | `E:/project/.worktrees/window-a` |
| 生命周期 | 永久 | 用户关闭时清理 |

**Agent 如何知道自己在真窗口还是虚拟窗口？**

**答案**：Agent **不需要知道**。

- Server 启动 Agent 时已经设置好工作目录上下文
- `ai_job_create` 不传 `baseBranch` 时，Server 自动获取当前分支
- 合并操作由用户通过 Web UI 触发，Web 知道窗口类型

---

## 四、Server 内部处理

### 4.1 自动存档逻辑

在 `CreateAiJob` API 中实现：

```csharp
// 自动存档：创建前检测到未提交更改，静默执行存档
if (_gitService.HasUncommittedChanges(projectPath))
{
    _gitService.Commit(projectPath, $"自动存档_{DateTime.Now:yyyyMMdd_HHmmss}");
    _logger.LogInformation("创建 AI Job 前自动存档");
}
```

### 4.2 获取当前分支逻辑

在 `CreateAiJob` API 中实现：

```csharp
// baseBranch 为空时自动获取当前分支
if (string.IsNullOrEmpty(request.BaseBranch))
{
    request.BaseBranch = _gitService.GetCurrentBranch(projectPath);
    _logger.LogInformation("自动使用当前分支作为基准: {Branch}", request.BaseBranch);
}
```

### 4.3 Complete API 内置 git commit

在 `CompleteAiJob` API 中实现：

```csharp
// 1. 在 Worktree 中提交未暂存的修改
var worktreePath = Path.Combine(_gitService.GetWorktreesDir(projectPath), name);
_gitService.TryCommit(worktreePath, $"AI Job 完成: {summary}");

// 2. 通知 Web 端
await _hubContext.Clients.All.SendAsync("AiJobCompleted", new
{
    Name = name,
    Summary = summary,
    BranchName = branchName
});
```

---

## 五、已有 Server REST API

> 来源：`GitController.cs` 已实现

| 端点 | 方法 | 功能 | Agent 需要 |
|------|------|------|-----------|
| `api/git/status` | GET | 获取工作区状态 | ⚪ 可选 |
| `api/git/current` | GET | 获取当前分支 | ⚪ 可选（ai_job_create 内部调用） |
| `api/git/branches` | GET | 获取所有分支列表 | ⚪ 可选 |
| `api/git/worktrees` | GET | 获取 Worktree 列表 | ⚪ 可选 |
| `api/git/ai-job` | POST | 创建 AI Job | ✅ **必需** |
| `api/git/ai-job/{name}/complete` | POST | 标记 AI Job 完成 | ✅ **必需** |
| `api/git/commit` | POST | 提交更改 | ❌ Agent 不调用 |
| `api/git/merge` | POST | 合并分支 | ❌ Agent 不调用 |
| `api/git/worktrees` | POST | 创建 Worktree | ❌ Agent 不调用 |
| `api/git/worktrees/{name}` | DELETE | 删除 Worktree | ❌ Agent 不调用 |
| `api/git/checkout` | POST | 切换分支 | ❌ Agent 不调用 |
| `api/git/discard` | POST | 放弃更改 | ❌ Agent 不调用 |

---

## 六、用户操作（Web UI）

### 6.1 合并流程

用户点击【合并】按钮后：

```
POST /api/git/merge
{
  "sourceBranch": "feat/layout-job-1-20260120-xxx",
  "targetBranch": "master"           // 真窗口
  // 或 "worktreeName": "window-2"   // 虚拟窗口
}
    │
    ▼
DELETE /api/git/worktrees/layout-job-1?deleteBranch=true
    │
    ▼
Canvas 刷新，显示合并后的结果
```

### 6.2 丢弃流程

用户点击【丢弃】按钮后：

```
DELETE /api/git/worktrees/layout-job-1?deleteBranch=true
    │
    ▼
AI Job 的 Worktree 和临时分支被删除
```

---

## 七、总结

**Agent 只需 2 个 MCP 工具**：

| 工具 | 用途 | 调用时机 |
|------|------|----------|
| `ai_job_create` | 创建隔离环境 | execute 任务开始时 |
| `ai_job_complete` | 标记完成，通知 Web 端供用户审查 | SubAgent 完成后 |

**设计优势**：

| 优势 | 说明 |
|------|------|
| **Agent 逻辑简单** | 只关注"创建环境 → 执行任务 → 通知完成" |
| **用户保持控制权** | 决定是否接受 AI 的修改 |
| **Server 处理复杂性** | Agent 无需关心 Git 操作细节 |

---

## 版本历史

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v1.1 | 2026-01-20 | 扩展业务场景：从 3 个简略场景扩展为 7 个完整场景全景图 |
| v1.0 | 2026-01-20 | 初版：定义 Agent Git 工具体系 |
