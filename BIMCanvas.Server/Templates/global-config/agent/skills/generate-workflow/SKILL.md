---
name: generate-workflow
description: |
  BIMCanvas 完整布置工作流。五阶段框架：感知→规划→尝试↔修改→优化→汇报。
  当用户需要"布置"、"设计"、"创建"、"生成"、"规划"等完整布置操作时使用。
  当用户附带参考图片并需要"识别"、"落地"、"照这个来"、"参考这个"、"按这张图"、"手绘"、"草图"、"照着做"、"还原"等操作时也使用本工作流。
---

# 布置工作流

```
1.感知 → 2.规划(2.1→2.2→2.3) → 3.尝试↔修改 → 4.优化 → 5.汇报
 加载      语义方案 v0.1→v0.3     坐标+验证       设计品质      交付
 上下文    熵减：粗→细            Layer 1 闭环     Layer 2 改善
```

Stage 2 有两条路径（Stage 1 感知后判定）：
- **推导路径**（默认）：从空间约束主动推导布局方案
- **识别路径**：从用户附带的参考图片中提取布局意图

> 判定依据：用户消息中附带了参考图片（手绘草图或 CAD 平面图）→ 识别路径。无图片 → 推导路径。

---

## 语义方案

语义方案是**持续演进的决策文本**——用自然语言描述家具与墙面的语义映射（"床在西墙"、"衣柜沿北墙线性排列"），不是精确坐标（那是 modules.json 的职责）。

### 语义方案提交

**【必须】每完成一个子阶段（2.1/2.2/2.3），调用 `save_semantic_plan` 提交当前版本。**

WHY：工具调用是阶段完成的唯一标志。未调用 = 未完成，不得继续下一子阶段。提交行为将你的决策从内部草稿变为外部锚点——已提交的决策是后续推进的基础，不再重新推导。

### 演进规则

```
v0.1  空间骨架     动线方向 + 纵深层次 + 采光轴 + 初步设计意图     （Stage 2.1）
v0.2  主体框架     主要家具墙面归属 + 分区结论 + 设计目标          （Stage 2.2）
v0.3  完整方案     可选家具 + 设计目标细化 + 全部确认              （Stage 2.3）
```

识别路径的语义方案记录**翻译进度**而非设计决策：

```
v0.1  视觉解读     图中看到了什么 + 场景判定(A/B) + 户型差异分析     （识别 2.1）
v0.2  完整映射     模块选型 + 墙面归属 + 适配调整 → 全部确认          （识别 2.2）
```

→ v0.2 完成后直接进入 Stage 3（坐标化是 Stage 3 的职责，不属于语义方案）

确定性标记：
- `✓` 已确认——不再变动（v0.2 提交后主要家具即为 ✓）
- `→ 待用户确认` 战略选择，需要 AskUserQuestion

### 示例 1：L 形主卧

**空间**：L 形，主体 3300×4850mm，延伸 1700×1550mm（东北角），北墙入口(d_3)偏西，南墙 2000mm 窗，延伸区连通卫生间(d_7)。

#### v0.1

> ↓ Stage 2.1 完成后，调用 save_semantic_plan(zoneId, "v0.1", 以下内容) ↓

```
## 语义方案 · 主卧 rz_3 · v0.1

空间：L 形，主体 3300×4850，延伸 1700×1550（东北角，含卫生间门 d_7）
动线：北墙门 d_3（偏西）→ 南向，自然朝南窗行走
纵深：北 = 入口/过渡，南 = 窗前/私密
采光：南窗 2000mm，光线 S→N
初步意图：入口侧承载过渡和收纳，深处是睡眠核心
```

#### v0.2

> ↓ Stage 2.2：在对话中展开分析，然后调用 save_semantic_plan(zoneId, "v0.2", 以下内容) ↓

**床头墙快速筛选**：
候选：西墙 4850mm ✓ | 东墙₁ 3300mm ✓ | 北墙 排除（门段+面向窗）
→ 推荐西墙（最长，侧对南窗，远离入口）→ 待用户确认

**衣柜**：北墙门右段 ~3770mm，入口侧，隐私序列正确
**分区**：竖切打断北墙 → 淘汰 | 横切增加复杂度无收益 → 不分割

```
## 语义方案 · 主卧 rz_3 · v0.2

（空间/动线/纵深/采光：同 v0.1）

[新增] 主要家具（物理约束确定）
  床 ✓ → 西墙：4850mm 最长完整实墙，侧对南窗，远离入口
  衣柜 ✓ → 北墙门右段：~3770mm 线性，入口侧，隐私序列正确

[新增] 分区结论（家具位置过滤）
  竖切(X=12400)：打断北墙连续性 → 衣柜无法布置 → 淘汰
  横切(Y=4200)：可行但增加复杂度，收益不明显
  不分割：可行，延伸区自然承载辅助功能
  → 结论：不分割。延伸区功能在 2.3 明确

[新增] 设计目标
  · 入口 → 更衣区 → 睡眠区的隐私递进 (空间节奏)
  · 北墙连续存储叙事墙 (功能叙事)
```

#### v0.3

> ↓ Stage 2.3 完成后，调用 save_semantic_plan(zoneId, "v0.3", 以下内容) ↓

```
## 语义方案 · 主卧 rz_3 · v0.3

（空间/动线/纵深/采光：同 v0.1）
（分区：不分割）

主要家具
  床 ✓ → 西墙，面朝东，成对床头柜
  衣柜 ✓ → 北墙线性 ~3770mm（不做 L 型延伸）

[新增] 可选家具
  梳妆台 ✓ → 延伸区东墙₂（~950mm，紧邻卫浴门）
    ★ 用户选择：组合布置方案（衣柜+梳妆台形成 L 区域 > 独立靠窗采光）
  窗帘 ✓ → 南墙全宽
  generalStorage → 不配置（衣柜已充足）

设计目标（完整）
  · 入口 → 更衣区 → 睡眠区的隐私递进 (空间节奏)
  · 北墙连续存储叙事墙 (功能叙事)
  · [新增] 延伸区 = 梳妆 + 卫浴过渡 (空间意图)
  · [新增] 南窗采光轴无高家具遮挡 (采光通风)
```

### 示例 2：窄长卫生间

**空间**：矩形 1540×2540mm，南墙入口(d_2)偏东，北墙高窗。

#### v0.1

> ↓ Stage 2.1 完成后，调用 save_semantic_plan(zoneId, "v0.1", 以下内容) ↓

```
## 语义方案 · 公卫 rz_2 · v0.1

空间：矩形 1540×2540，短边 ≤ 1800mm（窄长型）
动线：南墙门 d_2（偏东）→ 北向
纵深：南 = 入口/干区，北 = 深处/湿区
采光：北墙高窗（通风排湿）
初步意图：单侧排布，对侧留通道
```

#### v0.2

> ↓ Stage 2.2 完成后，调用 save_semantic_plan(zoneId, "v0.2", 以下内容)（卫生间是模板匹配型空间，分区 + 家具一步到位）↓

```
## 语义方案 · 公卫 rz_2 · v0.2

（空间/动线/纵深/采光：同 v0.1）

分区：不分割（矩形 + 单功能）

模式判断：A（单侧线性）
  门在南墙偏东 → 入门开阔侧在西 → 洁具沿西墙
  台盆 ✓ → 西墙南端，贴靠南墙角
  坐便器 ✓ → 台盆北侧，可用段居中
  淋浴 ✓ → 西墙北端，mod_shower_linear，宽度 = 1540mm
  通道 → 东侧保留 ≥ 700mm

设计目标
  · 进门左转直达台盆 (动线设计)
  · 西墙统一洗漱 + 如厕功能 (功能叙事)
  · 洗手 → 如厕 → 淋浴由浅入深 (空间节奏)
```

> 卫生间的 v0.2 已接近完整方案——卫生间是"模板匹配型"空间，决策空间小，v0.3 仅补充配件。

### 示例 3：同户型草图落地（识别路径 · 场景 A）

**输入**：用户附带一张手绘草图 + 消息"照这张草图来布置"。草图画的是当前户型 rz_3（3300×4850mm 矩形主卧，北墙门，南墙窗）。草图中：大矩形靠西墙（床）、床两侧小方块（床头柜）、北墙长条（衣柜）、东南角小矩形（不确定）。

#### v0.1

> ↓ 识别 2.1 完成后，调用 save_semantic_plan(zoneId, "v0.1", 以下内容) ↓

```
## 识别方案 · 主卧 rz_3 · v0.1

场景类型：A（同户型 — 草图轮廓和开口位置与 zone 边界吻合）
置信度：高（草图比例与实际房间基本吻合，家具辨识清晰）

识别清单：
  1. 大矩形 → 双人床，靠西墙居中，面朝东
  2. 床两侧小方块 → 床头柜 ×2，紧贴床头
  3. 北墙长条 → 衣柜，沿北墙门右侧展开
  4. 东南角小矩形 → 不确定：梳妆台 or 书桌 → 待用户确认

空间关系：
  · 床居于房间中心偏西，主动线在东侧
  · 衣柜在入口侧，符合"进门即收纳"
```

#### v0.2

> ↓ 用户确认"东南角是梳妆台"后，识别 2.2 完成 ↓

```
## 识别方案 · 主卧 rz_3 · v0.2

（场景/识别清单：同 v0.1，歧义已解决）

模块映射：
  床 → mod_bed_001（1800×2100）
    WHY：草图比例接近 1800 宽度，房间 3300mm 容许
  床头柜 → mod_nightstand_001 ×2（500×400）
  衣柜 → mod_wardrobe_linear_001（深 600mm）
    WHY：北墙门右段约 2200mm，线性最匹配草图形状
  梳妆台 → mod_vanity_001（1000×450）
    WHY：东南角空间 + 用户确认
```

### 示例 4：跨户型参考图重建（识别路径 · 场景 B）

**输入**：用户附带一张 CAD 平面图 + 消息"参考这个图来布置"。CAD 图是另一户型主卧（约 3800×5200mm），布局：床靠南墙、L形衣柜占西墙+北墙转角、东墙书桌。当前户型 rz_3 是 3300×4850mm，北墙门偏西，南墙窗 2000mm。

#### v0.1

> ↓ 识别 2.1 完成后 ↓

```
## 识别方案 · 主卧 rz_3 · v0.1

场景类型：B（跨户型 — 参考图 3800×5200 vs 当前 3300×4850，比例和尺寸显著不同）
户型差异：
  参考：3800×5200，门位不明
  当前：3300×4850，北墙门偏西，南墙窗 2000mm
  差异：宽度少 500mm，深度少 350mm，当前南墙有窗

识别清单：
  1. 床 → 靠南墙居中，面朝北
  2. L形衣柜 → 西墙全长 + 北墙转角延伸
  3. 书桌 → 东墙中段

⚠ 适配预警：
  · 床靠南墙 → 当前南墙有窗，违反避窗原则
  · L形衣柜西墙段需缩短（4850 vs 5200）
  · 宽度少 500mm → 需验证通道
```

#### v0.2

> ↓ 识别 2.2：用户确认床迁到西墙后 ↓

```
## 识别方案 · 主卧 rz_3 · v0.2

模块映射 + 适配调整：
  床 → mod_bed_001（1800×2100）
    适配：南墙有窗 → 迁移到西墙
    WHY：床头靠窗影响睡眠和窗帘使用
  衣柜 → mod_wardrobe_linear_001（线性，放弃 L 形）
    适配：床占西墙 → L 形不可行 → 北墙线性
  书桌 → mod_desk_001（1200×600）
    适配：位置保持东墙，需验证通道

⚠ 床从南墙迁到西墙 = 对参考意图的重大调整 → 已通过 AskUserQuestion 确认
```

---

## 1. 感知

> WHY：纯机械操作，不占认知资源。先建立感官输入，后续阶段才有决策素材。

1. **截图**（单独调用，等收到后再继续）— `mcp__canvas__request_background_screenshot`，用 `viewport: {"id": "rz_1"}` 聚焦目标区域，或省略 viewport 看全屋。截图工具直接返回图片；若看不到内容，用 Read 查看返回的文件路径
2. **并行读取**：references/design_principles.md + references/design_evaluation.md + modules/module_library.json + schemes/zones.json + computed/exclusions.json
3. **边界语义**：调用 `get_zone_boundaries` — 获取每条边的类型（wall/passage/door/window）和坐标。后续所有阶段基于此墙面语义描述空间，不回溯 rawBoundary 原始坐标
4. **加载房间策略**：根据 Zone tags 读取对应策略参考文件（见系统注入的"策略参考文件"路径）

| tags 特征 | 空间类型 | 策略文件 |
|-----------|---------|---------|
| sleep / bedroom | 卧室 | bedroom.md |
| shower / toilet / washing | 卫生间 | bathroom.md |
| rest / tvMedia / dining / circulation | 客餐厅 | livingroom.md |

WHY（边界语义）：`get_zone_boundaries` 返回的是**预处理的墙面结论**（"北墙门右段 4070mm"），而非原始坐标列表。当数据本身是结论形态时，Agent 在物理上无法"重新分析"——这是结构性防重分析措施。

**模块数据**：契约层（id/tags/size）用于选择，意图层（agent_config: morphology + topology_rules + relation_rules）用于布置决策。按 Zone 的 tags/optionalTags 筛选兼容模块——匹配 tags 为必备家具，仅匹配 optionalTags 为可选家具。

---

## 2. 规划

> **规划 = 分阶段提交。** 每个子阶段完成后调用 `save_semantic_plan` 提交决策。
> 提交后的决策是后续的锚点，不再重新推导。

### 路径判定

用户消息中附带了参考图片 → **识别路径**（见下方）。无图片 → **推导路径**（2.1/2.2/2.3 按现有流程执行）。

> WHY：同一套语义方案框架，不同的信息来源。推导路径从空间约束主动推导（v0.1→v0.3）；识别路径从图片被动提取（v0.1→v0.2）。两条路径的语义方案最终版本是 Stage 3 的统一入口——后续 Stage 3/4/5 完全相同。

---

### 识别路径

> 你是翻译官，不是设计师。图片中的布局意图是你的"原文"，modules.json 是你的"译文"。忠实传达原意，不做二次创作——即使你认为有更好的方案。
>
> WHY：用户选择附带参考图片，说明他们已经有了设计意图。尊重这个意图是首要任务。翻译允许"信达雅"的补全，但不允许改写原意。

#### 识别 2.1 视觉理解 → v0.1

观察用户附带的图片，产出结构化的视觉解读（参见示例 3、4）。

**【必须】先验证，再分类**：对比图片中的房间轮廓（墙数、转角、大致比例）和开口位置（门窗数量、相对位置）与 `get_zone_boundaries` 数据。v0.1 中必须输出对比证据。

WHY：图片类型（手绘/CAD/截图）不决定场景分类——同一户型的 CAD 截图是场景 A，不同户型的手绘草图是场景 B。只有户型匹配结果决定分类。

**场景判定**（基于上述验证结果）：
- **场景 A**（同户型）：图片中的房间与当前 zone 的轮廓和开口位置基本吻合 → 直接映射
- **场景 B**（跨户型）：图片中的房间与当前 zone 存在结构性差异（墙数/比例/开口位置明显不同）→ 需要适配

**场景 B 才需要的额外步骤**：标注差异点和潜在冲突（哪些家具位置因户型差异需要调整）。

WHY：户型差异分析是场景 B 的专属需求——场景 A 不存在户型差异，不需要适配分析。

**【必须】歧义不猜测**：无法确定的家具标注为"不确定"，在进入 2.2 前通过 AskUserQuestion 向用户确认。

WHY：手绘草图中一个模糊矩形是衣柜还是书柜，决定了整面墙的功能定位。翻译官猜错一个词可能导致整段意思偏离。

**【必须】** 调用 `save_semantic_plan(zoneId, "v0.1", 方案文本)` 提交 v0.1。

#### 识别 2.2 完整映射 → v0.2

将 v0.1 识别的每件家具映射到 `module_library.json`，确定全部墙面归属。

**映射依据**（按优先级）：
1. 视觉特征（形状、比例、草图中的大小关系）
2. Zone tags 语境（卧室里的大矩形更可能是衣柜而非书柜）
3. 模块尺寸与房间尺寸的兼容性

**场景 B 适配规则**：

| 冲突类型 | 处理 |
|----------|------|
| 家具位置与门窗冲突 | 迁移到最近可用墙段 |
| 墙段长度不足 | 选更小规格模块 |
| 总宽度不足 | 验证通道，必要时缩减可选家具 |

**【必须】场景 B "重大调整"需用户确认**：适配导致家具更换墙面（不是同墙微调）时 → AskUserQuestion。

WHY：用户给你参考图是希望"照着来"。把床从南墙挪到西墙是对原意的改写，用户有权知道并同意。

**【必须】** 调用 `save_semantic_plan(zoneId, "v0.2", 方案文本)` 提交 v0.2。

**【必须】** 提交 v0.2 后，读取 `references/optional-furniture-rules.md`（与推导路径相同）。

→ v0.2 提交后，直接进入 Stage 3（与推导路径完全相同）。

**识别路径不调用 generate-zoning Skill**。分区信息（如果有）从图片中提取，不做主动分区评估。

WHY：被动设计尊重参考图片的空间组织方式。如果参考图中没有分区，就不分区；如果有，在 v0.1 视觉解读中记录。主动分区评估属于"二次创作"，违背翻译官定位。

---

### 推导路径

#### 推导 2.1 基础规划 → 语义方案 v0.1

以 design_evaluation.md 的品质维度为指引，完成空间的初步阅读：

- **动线方向**：人从入口进来自然往哪走？
- **纵深层次**：哪里是前场（过渡），哪里是深处（私密）？
- **采光轴**：窗户在哪，光怎么进来？
- **初步设计意图**：这个空间应该有怎样的体验序列？

产出语义方案 v0.1——只有空间骨架，不涉及任何家具决策。

**【必须】**调用 `save_semantic_plan(zoneId, "v0.1", 方案文本)` 提交 v0.1。

#### 推导 2.2 分区设计 → 语义方案 v0.2

**【必须】加载分区 Skill**：加载 generate-zoning Skill，完成空间功能定义。

WHY：分区 Skill 不只是处理"复杂空间"——它负责空间功能定义。"不分割"是合法结论，不是跳过分区的理由。功能定义是布置的前提，需要经过显式审视才能确立。Skill 基于 v0.1 已产出的空间骨架做判断，不重新推导空间信息。

执行顺序——**先家具，再分区**：

**Step 1：确定主要家具的最优位置**

在不考虑分区的前提下，用房间策略的优先级规则确定主要家具应该在哪面墙。

WHY：家具位置由**物理约束**（墙面长度、门位置、窗位置）决定，这些约束来自空间本身，不来自概念上的分区划分。无论怎么分区，西墙还是最长的实墙，北墙右段还是最长的非窗非门段——物理事实不因概念组织而改变。

**Step 2：用家具位置过滤分区方案**

确定了主要家具需要哪些墙面资源后，检查哪些分区方案与这些需求兼容：

- 分区切割打断了家具需要的连续墙面？→ 该分区方案被淘汰
- 分区后家具仍能正常布置？→ 该方案可行，保留

这是**单向约束**：物理空间 → 家具最优位置 → 过滤分区方案。不存在循环依赖。

**Step 3：确定分区结论 + 功能定义**

- 多个可行方案对用户日常体验有质的差异 → 战略选择 → AskUserQuestion
- 确定分区（或"不分割"）后，进行功能定义
- 产出语义方案 v0.2：分区结论 + 主要家具决策 + 设计目标

**【必须】**调用 `save_semantic_plan(zoneId, "v0.2", 方案文本)` 提交 v0.2。

**【必须】**提交 v0.2 后，立即读取 `references/optional-furniture-rules.md`。

WHY：Read 调用是进入 Stage 2.3 的入口——它携带了可选家具阶段的工作边界和禁止回流规则。读取后按照该文件的指引开始 v0.3 分析。

→ Skill 产出 subZones 时：调用 `get_zone_boundaries` 获取子 zone 边界语义（含 passage 段）
→ Skill 判定无需分区时：直接继续

**【必须】主家具确认原则**：v0.2 中主要家具标记为 `✓`（确认）。v0.2 提交后，主要家具的墙面归属不可推翻。v0.3 可在不改变主要家具位置的前提下扩展（如衣柜从线性延伸为 L 型），但不可回溯主要家具决策。

#### 推导 2.3 丰富细节 → 语义方案 v0.3

将主要家具之外的家具纳入语义方案：

- 可选家具（梳妆台、展示柜等）：扫描候选墙段，标注位置
- 附属家具（窗帘、床头柜等）：跟随锚点家具
- 判断是否存在战略选择（可选家具位置影响日常体验）→ AskUserQuestion

**决策收敛**：列出全部选择点，逐一标注"战略/战术"：
- **战略选择**（家具墙面归属、空间功能定位等，影响日常体验）→ AskUserQuestion
- **战术选择**（间距微调、同墙面位置偏移等，由规范决定）→ 自主决策

> 判断标准：
> - **战术**：硬约束直接排除至仅剩一个可行方案，或差异仅涉及精确坐标/间距调整
> - **战略**：存在两个及以上可行方案，且差异涉及用户日常体验

**【必须】唯一解回溯检查**：当某选择点"只剩一个可行方案"时，检查该唯一性的来源——是硬约束（空间物理限制、【必须】规则）直接排除的，还是前序决策间接消除的？

- 硬约束排除 → 战术，自主决策
- 前序决策消除 → 回到前序决策，评估是否存在"调整前序决策为当前家具腾出替代位置"的方案。如果存在，当前选择点是战略选择

WHY：前序决策消除替代选项后标注"唯一解=战术"，是自证预言——先做了一个选择，再用它的结果证明后续没有选择。回溯检查打断这条错误推理链。

> ⚠ 两个检验信号：(1) 在 thinking 中花费显著推理论证一个方案优于另一个——取舍 = 战略选择的特征；(2) "唯一可行位"的唯一性依赖于前序决策而非空间物理限制——自证预言 = 分类错误的特征。

v0.3 完成后，所有位置升级为 `✓`（确认）。此时语义方案包含：全部家具墙面归属、设计目标声明（参照 design_evaluation.md 品质维度）。

**【必须】**调用 `save_semantic_plan(zoneId, "v0.3", 方案文本)` 提交 v0.3。

> WHY（设计目标）：设计目标不是额外负担——它是后续执行和审查的锚点。有了"进门直达台盆"这个目标，选墙时就不需要纠结公式；审查时只需验证"进门后首个使用点是台盆吗？"。目标驱动让每个决策都有判据。

---

## 3. 尝试 ↔ 修改

> 语义方案最终版本（推导路径 v0.3 / 识别路径 v0.2）已确定了每件家具的墙面归属和大致位置。
> Stage 3 的任务是把语义描述转化为坐标——写入 modules.json，让 validate_layout 校对。
>
> **【姿态】快速落地。** validate_layout 比心算更快更准——写入一个合理的草稿，让工具告诉你哪里需要修正。首版写入后被 validate_layout 报错是正常流程，不是失败。
>
> WHY：快速迭代（写入→验证→修正）的总成本远低于在写入前预防所有问题的成本。

### 手动检查清单

Agent 在写入前**只需**检查以下 validate_layout 无法检测的语义问题，不做其他预验证：

| 检查项 | WHY validate_layout 检测不到 |
|--------|---------------------------|
| 门开启方向与家具是否冲突 | validate_layout 检查门扇禁区重叠，但不理解"门能不能顺畅打开" |
| 通行间隙 vs 使用间隙的类型区分 | validate_layout 不区分"人要从这里走过"和"人在这里伸手操作" |
| 前瞻协调：主家具是否阻断可选家具的策略位置 | 这是语义层面的设计意图，不是几何计算 |

**其余全部交给 validate_layout**：越界(E001)、墙体重叠(E002)、柱子重叠(E003)、禁区重叠(E004)、模块间重叠(E005)。

### 试错节奏示例

> 先给出满足朝向与贴墙关系的粗坐标，碰撞与净距留给 validate_layout 检查。

```
Write modules.json（床靠西墙，衣柜靠北墙，按语义方案逐件定位）
→ validate_layout → E005: 衣柜西端与床使用侧柜重叠 85mm
→ 衣柜整体东移 100mm → Write → validate_layout → 0 errors
```

一次修正即完成。不需要在写入前预先计算每对家具之间的精确间距——validate_layout 会告诉你哪里撞了、撞了多少。

### 放置

按语义方案 v0.3 + design_principles + 房间策略约束 + module_library agent_config，精确定位全部家具（锚点→主要→可选，一次性写入）。

### 写入

Write `schemes/{zoneId}/modules.json`，每个模块包含 moduleId、bounds（四点坐标）、facing、items。
分区场景写入路径：`schemes/{parentZoneId}/{childZoneId}/modules.json`（如 `schemes/rz_3/dz_1/modules.json`）。

**facing 规则**：
- `facing` 写成对象：`{ "value": [x, y] | null, "semantic": string | null }`
- `value` 是常规读取阶段的方向真理；`semantic` 是 AI 语义输入槽，只接受 8 个标准方向词
- **推荐**AI 默认修改 `semantic`。示例：`"facing": { "value": null, "semantic": "south" }`
- 若 `value` 与 `semantic` 同时存在：常规读取只认 `value`；`validate_layout` 会用有效 `semantic` 覆盖 `value`，再把 `semantic` 清空为 `null`

### 验证闭环

**【必须】**每次 Write 后调用 `mcp__canvas__validate_layout(zoneIds=[...])`。分区场景传入子 zone 列表（如 `zoneIds=["dz_1", "dz_2"]`）。

WHY：validate_layout 是确定性几何计算，比 LLM 心算更快更可靠。它是"允许试错"姿态的安全网——放心写入，让工具替你检查。

### 修正循环（最多 2 轮）

验证失败时：
1. 列出所有违规项
2. 按修正优先级处理（见 design_principles）：**平移** → **旋转** → **缩小**（limits 内）→ **拆除附属件** → **替换**同功能更小模块 → **移除**
   - 卧室睡眠组例外：最低合法组合是 `mod_bed_002 + 单床头柜`。若已降到该组合仍失败，不自动降为仅床或移除床
   - **跨墙面迁移 = 推翻已确认决策**：修正循环中将家具从 v0.2/v0.3 确认的墙面迁移到其他墙面，等同于回溯语义方案。必须先穷尽当前墙面内的所有修正手段（平移、旋转、缩小、拆除附属件），全部失败后才可考虑跨墙面迁移，且识别路径下需通过 AskUserQuestion 确认
3. Read 当前 modules.json → 修正 → Write → 再次 validate_layout

> WHY：修正优先级从最小变动到最大变动——平移保留原始设计意图，移除是最后手段。睡眠组额外设最低合法组合，是为了避免 AI 通过偷降床头柜数量掩盖空间事实。

**兜底**：2 轮修正后仍失败 → 非睡眠核心家具可移除；若卧室睡眠组连 `mod_bed_002 + 单床头柜` 仍失败，报告该房间物理上无法满足最低睡眠组合，不自动降为仅床或移除床。

### Layer 1 验证

validate_layout 通过后，继续完成工程合规验证：

1. **可达性验证**（【必须】用坐标算术证明，禁止凭印象声称"满足"）：
   从入口走到每件家具使用面和每扇窗户，逐段计算最窄处，比对 design_principles 通道标准和房间策略专项标准。
   报告格式（【必须】逐条列出，禁止用表格替代——逐条格式展示每条通道的推理链，表格隐藏设计根因）：
   `[起点] → [终点]：最窄处 [物体A] ↔ [物体B] Xmm（标准≥Ymm）✓/✗`
   示例：`[门] → [台盆使用面]：最窄处 台盆 ↔ 西墙 1050mm（标准≥900mm）✓`
2. **功能完整性**：zone.tags 中每个功能标签至少有一个对应模块 → 缺失则补充

Layer 1 任何项未通过 → 修正（最多 2 轮）→ 重新验证
全部通过 → 进入 Stage 4

---

## 4. 优化

> WHY：Layer 1 通过 = 工程合规（能用）。优化阶段的目标是"从合格到优秀"——主动寻找改善机会，不是被动检查是否达标。

> **识别路径**：默认跳过优化阶段，Stage 3 通过后直接汇报。但如果 Stage 3 修正过程中发现图片意图有明显不合理（如通道过窄、功能缺失），向用户说明并询问是否需要优化调整。

以下为推导路径的优化流程（识别路径按需参考）：

**截图**（单独调用）→ 逐项审查：

对照语义方案 v0.3 中声明的设计目标，参照 design_evaluation.md 的五个品质维度：
- 声明了动线目标？→ 验证入门到首个使用点的路径
- 声明了功能叙事目标？→ 验证墙面/区域的家具连贯性
- 声明了空间节奏目标？→ 验证隐私/密度递进
- 声明了采光通风目标？→ 验证采光轴畅通
- 其他已声明的目标？→ 逐条核实

识别改善机会 → 每个维度最多一次调整尝试 → 调整后重新 validate_layout 确认工程合规

> 典型优化场景：梳妆台布置在远离窗户端，导致主动线在中间被挤压。移到靠窗端后，挤压只发生在动线末端，影响大幅降低。原布局完全合规——这是"合格到优秀"的品质提升。

**贯穿检查**：
- **策略一致**：最终布局实现了语义方案的核心意图
- **规则遵从**：房间策略的硬约束全部满足
- **分区连贯**（分区场景）：相邻子分区之间的动线连贯

**降级判断**：截图无法清晰辨别时，注明"品质评估依赖数字验证 + 策略交叉检查"。

全部通过 → 汇报

---

## 5. 汇报

仅在 Stage 3 合规 + Stage 4 优化完成后汇报：
- 空间画像摘要（动线、纵深、采光）
- 策略要点（锚点决策、关键取舍）
- 放置结果（家具清单、位置、朝向）
- 品质评估（Layer 1 合规 + Layer 2 设计目标达成情况）
- 决策痕迹：分歧点+选择理由（如有）

识别路径额外包含：
- 识别摘要（图片中看到了什么）
- 适配说明（场景 B：做了哪些调整、为什么）

**禁止**：审查未通过时报告"布置完成"。

---

## 机制速查

- **【必须】截图单独调用**：不与其他工具并行
- **修正优先级**：平移 → 旋转 → 缩小 → 拆除附属件 → 替换 → 移除（见 design_principles）；卧室睡眠组最低保留到 `mod_bed_002 + 单床头柜`
- 先读后写、validate_layout 必调、目录权限 → 见 BIMCANVAS.md 安全机制
- **识别路径**：有参考图片时走识别路径，翻译官定位——先验证户型匹配再分类（同户型=A/跨户型=B），忠实还原图片意图，歧义问用户，场景 B 换墙需确认，默认跳过优化
