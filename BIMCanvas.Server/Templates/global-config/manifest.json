{
  "version": "1.0",
  "comment": "组5 §5.A.6 修订:agent/BIMCANVAS.md / agents/ / skills/ / .claude-plugin 已迁到 plugins/indoor-layout/(由 core-base 简化骨架替代);本 manifest 只保留平台级配置(server/* + agent/config.json)。agent prompt + SubAgent + Skills 由 EnsurePluginInitialized('core-base') 物化到 BIMCANVAS_HOME/plugins/core-base/。v3.6 修订:PLATFORM_CONTRACT.md 内容并入 core-base/BIMCANVAS.md,本 manifest 不再分发该文件(归档参考: .dev/docs/Platform_Contract_Reference.md)。",
  "items": [
    {
      "name": "server/server_config.json",
      "target": "server_config.json",
      "type": "template",
      "enabled": true,
      "description": "Server 启动配置（Server/Web/Agent/CCR 端口、浏览器）"
    },
    {
      "name": "server/web_config.json",
      "target": "web_config.json",
      "type": "template",
      "enabled": true,
      "description": "Web 客户端配置（自定义模型列表、图层预设）"
    },
    {
      "name": "server/ccr_config.json",
      "target": "ccr_config.json",
      "type": "template",
      "enabled": true,
      "description": "Claude Code Router 配置（唯一 API 网关）"
    },
    {
      "name": "agent/config.json",
      "target": "config.json",
      "type": "template",
      "enabled": true,
      "description": "Agent runtime 全局配置（claude / openai providers / permissions / modelMapping）"
    }
  ]
}
