# BIMCanvas Docker server deployment variables
# Copy this file to deploy/.env before running the server overlay compose command.

# Shared host data root. Each instance stores runtime state under:
#   /srv/bimcanvas-data/instance1
#   /srv/bimcanvas-data/instance2
BIMCANVAS_DATA_ROOT=/srv/bimcanvas-data
